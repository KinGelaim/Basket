<IfModule mod_rewrite.c>
    <IfModule mod_negotiation.c>
        Options -MultiViews -Indexes
    </IfModule>

    RewriteEngine On

    RewriteCond %{REQUEST_URI} (.*)
    RewriteRule ^(.*)$ /kontrol_order/public/$1 [L,R=301]
</IfModule>
